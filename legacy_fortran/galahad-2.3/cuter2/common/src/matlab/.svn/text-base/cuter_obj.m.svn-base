function [varargout] = cuter_obj( varargin )
% Return function value and gradient if requested.
% Usage:  f = cuter_obj(x)  or  [f,g] = cuter_obj(x)
    varargout = cell(1,nargout);
    [varargout{:}] = mcuter('obj',varargin{:});
